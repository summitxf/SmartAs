{
    "name": "my-react",
    "version": "1.0.0",
    "description": "react wegits",
    "scripts": {
        "build": "babel src/main/resources/web  --compact true --minified  -w -x .jsx -d src/main/resources/web/"
    },

    "devDependencies": {
        "babel-preset-react": "^6.3.13"
    },

    "babel": {
        "presets": ["react"]
    }
}
